#include <stdio.h>

int main() {
    char name[] = "Alice";
    int age = 25;

    printf("name: %s\n", name);
    printf("age: %d\n", age);
    printf("%s is %d years old.\n", name, age);
    return 0;
}